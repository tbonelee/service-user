// Ambient globals for the UMD wrapper — used by `tsc --checkJs` only.
// Not published (see package.json "files"); keeps type-checking of the
// AMD branch (`typeof define === 'function'`) clean.
declare var define: any;
